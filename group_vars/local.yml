---
# [local] group: a workstation or test VM. Skip anything that rewrites the
# bootloader (the tuned profile generates the kernel command line on activation)
# and apply the sysctls through sysctl.d instead.
amd_low_latency_tuned_profile_enabled: false
housekeeping_sysctl_enabled: true
