---
# [remote] group: production hosts. Full tuned profile; set
# amd_low_latency_reboot_after_apply: true to reboot automatically when the
# profile (and hence the kernel command line) changes.
amd_low_latency_tuned_profile_enabled: true
amd_low_latency_reboot_after_apply: false
